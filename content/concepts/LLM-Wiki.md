---
title: LLM-Wiki
type: concept
tags: [knowledge-management, LLM, PKM, wiki, obsidian, agent-skills]
created: 2026-04-09
updated: 2026-09-03
sources: [llm-wiki-karpathy-2026.md, claude-obsidian.md]
---

# LLM-Wiki

> [!update] 2026-09-03 — 🚨 **이 볼트에 결측된 층이 확인됐다: 검색 피드백 루프** ([[CoGR]])
> [[CoGR]](HF 데일리 4위)은 검색을 이렇게 재정의한다 — **질의뿐 아니라 「아이템 측」의 검색 표현(키워드)도 LLM이 생성하고, 그 표현을 「질의 성공률의 반사실적 변화」로 학습시킨다.**
> **이 볼트가 정확히 그 구조다.** 915+개 소스 페이지를 `grep`과 wikilink로 찾고, 각 페이지의 **태그·제목·wikilink가 곧 아이템 측 생성 키워드**다 — 다만 **내가 수작업으로 생성**한다.
> 🚨 **그런데 볼트는 그 키워드가 유효했는지 한 번도 측정한 적이 없다.** 태그를 붙이기만 하고, **어떤 태그로 실제 페이지가 찾아졌는지** 기록하지 않는다. CoGR의 표현을 빌리면 **아이템 측에 보상 신호가 전혀 안 들어온다.**
> 원인이 구조적이다: **`queries/` 디렉터리가 0건**이다. 스키마에 *"좋은 답변은 queries/에 저장"* 이 있으나 **한 번도 실행되지 않았다.** 즉 볼트는 **인제스트(쓰기)만 하고 쿼리(읽기)를 기록하지 않는 半쪽 시스템**으로 5개월을 운영됐다.
> → **actionable 등재(우선순위 높음)**: 쿼리를 `queries/`에 남기기 시작한다. 그것 없이는 **어떤 태그가 죽은 태그인지 영원히 알 수 없다.**
> 📌 같은 배치 [[Repo-To-Skill]]이 **자산 5,000개를 만들고도 검색 방법을 안 적었다**는 사실과 짝을 이룬다 — **자산을 쌓는 쪽은 찾는 문제를 늦게 발견한다.** 볼트도 같은 순서를 밟고 있다.

LLM을 활용하여 개인 지식베이스를 **복리 축적형**으로 구축하는 패턴. [[Andrej Karpathy]]가 2026년 공개한 아이디어 가이드에서 비롯됨.

---

## 핵심 철학

기존 [[RAG vs LLM-Wiki|RAG]] 방식은 쿼리마다 원본에서 재추출한다 — 지식이 쌓이지 않는다.
LLM-Wiki는 다르다: LLM이 **영속적 위키를 점진적으로 구축·유지**한다.

> "The wiki is a persistent, compounding artifact. The cross-references are already there. The contradictions have already been flagged." — Karpathy

**인간의 역할**: 소싱, 탐색, 좋은 질문
**LLM의 역할**: 요약, 교차 참조, 정리, 북키핑 전체

---

## 3계층 아키텍처

| 레이어 | 소유자 | 역할 |
|--------|--------|------|
| Raw Sources | 인간 | 원본 소스. 불변. 진실의 원천. |
| Wiki | LLM | 마크다운 파일 모음. LLM이 작성·유지. |
| Schema | 인간+LLM | CLAUDE.md — LLM을 위키 관리자로 만드는 설정 |

---

## 3가지 핵심 작업

### Ingest (인제스트)
새 소스 추가 → LLM이 읽고 → 위키에 통합
- 단일 소스가 10~15개 페이지에 영향 가능
- entity 페이지, concept 페이지, 모순 표시 등 자동화

### Query (쿼리)
위키 대상으로 질문 → LLM이 관련 페이지 탐색 → 인용 포함 답변 합성
- **좋은 답변은 위키 페이지로 보존** → 탐색 자체도 지식 복리 축적

### Lint (린트)
주기적 위키 건강 점검:
- 페이지 간 모순
- 고아 페이지 (인바운드 링크 없음)
- 낡은 주장 (최신 소스에 의해 대체됨)
- 자체 페이지 없는 중요 개념

---

## 왜 작동하는가

> "LLMs don't get bored, don't forget to update a cross-reference, and can touch 15 files in one pass."

지식베이스 유지의 핵심 장벽은 **북키핑** — 교차 참조 업데이트, 요약 최신화, 모순 표시. 인간은 이 부담이 커지면 위키를 포기한다. LLM은 지루함을 모르고 한 번에 15개 파일을 처리한다.

---

## Vannevar Bush의 Memex와의 연결

1945년 Vannevar Bush가 꿈꾼 **Memex** — 개인적·능동적으로 큐레이션되며, 문서 간 연결이 문서 자체만큼 가치 있는 지식 저장소. Bush가 해결 못한 "누가 유지 관리하는가" 문제를 LLM이 담당한다.

---

## 실용 도구

- **Obsidian**: 위키 뷰어. 그래프 뷰, Dataview 플러그인, Marp 슬라이드
- **Obsidian Web Clipper**: 웹 기사 → 마크다운 변환
- **qmd**: 위키용 로컬 검색 엔진 (BM25+벡터 하이브리드)
- **Git**: 버전 관리, 브랜칭, 협업

---

## 2026-08-28 — 이 패턴이 **설치 가능한 레포**가 됐다 (첫 직접 비교군)

[[claude-obsidian]](AgriciDaniel/claude-obsidian · ⭐**14,215** · 포크 1,432 · MIT · Python · GitHub 트렌딩 14위)이 README에서 **명시적으로 밝힌다** — *"**Based on Karpathy's LLM Wiki pattern**"*.

**스택까지 이 볼트와 동일하다**: Obsidian + Claude Code + 순수 마크다운. 설계 주장도 겹친다 — *"임의의 소스를 투입하면(drop any source) 읽고 · 링크하고 · 정리한다"*, *"당신이 소유하는 하나의 연결된 순수 마크다운 지식그래프"*.

> [!insight] 이건 벤치마킹 대상이 아니라 **대조군**이다
> 같은 패턴을 남이 구현했을 때 **무엇이 자동화됐고 무엇이 여전히 판단으로 남았는지**가, 이 볼트 인제스트 루프의 **자동화 가능 상한**을 알려준다.
> 확인할 세 가지: (1) 소스 투입 → 페이지 생성까지 **사람 개입 지점** (2) **wikilink를 무슨 근거로 만드나**(문자열 매칭 / 임베딩 / LLM 판단) (3) **신규 페이지 vs 기존 갱신 판정**을 자동화했나.
> **(3)이 특히 중요하다** — 이 볼트에서 매 인제스트마다 손이 가장 많이 가는 판단이 정확히 그것이고, 900페이지가 넘는 규모에서 확장 한계에 부딪히는 지점도 그곳이다.

> [!warning] 자동화된 지식그래프의 진짜 실패 모드는 "안 도는 것"이 아니다
> [[claude-obsidian]]은 **정리 정확도를 재는 수치를 제시하지 않는다.** 그런데 지식그래프 도구의 지배적 실패는 *동작 실패*가 아니라 **그럴듯하게 잘못 연결하는 것**이고, 그건 스타 수로 절대 드러나지 않는다.
> 같은 날 배치의 [[PAWBench]]·[[FrontierChallenge]]가 겨누는 지점과 정확히 동형이다 — **산출물이 존재한다 ≠ 산출물이 옳다.**
> **이 볼트가 이미 하고 있는 방어**: `> [!warning]` 로 미검증 주장 분리 · API 실호출 재검증 · raw 수집 메모와 볼트 관측 계열이 충돌하면 **볼트 계열을 정본으로 채택**(2026-08-28 [[Qwen3.8-27B]] 사례). 자동 도구가 이런 판단을 하는지가 비교의 핵심이다.

**이슈 밀도 관측**: 이슈 140 / 포크 1,432 = **0.098건/포크**로 08-28 배치 GitHub 5건 중 **최고**([[awesome-gpt-image-2]] 0.0072의 **13.6배**). 볼트 규칙대로면 **가장 "실행되는 소프트웨어"에 가깝다**는 뜻이나, 마찰의 성격(버그 vs 기대 불일치)은 **이슈 내용 미확인**이다.

## 관련 페이지
- [[claude-obsidian]] — **이 패턴의 패키지화된 구현체이자 이 볼트의 직접 대조군** *(NEW 2026-08-28)*

- [[RAG vs LLM-Wiki]]
- [[Andrej Karpathy]]
