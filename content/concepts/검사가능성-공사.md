---
title: 검사 가능성 공사 (Inspectability Construction)
type: concept
domain: ai-news
tags: [interpretability, verification, agent, architecture, probe, reward-modeling]
created: 2026-09-02
updated: 2026-09-08
sources: [Qwen-Drive-1.0.md, Safin-1.md, UI-Venus-2.md, Agentic-Artifact-Creation.md, Aspire.md, HarnessDev.md, context7.md, Motion-Omni.md, WorldSculpt.md, Enoki.md, Ask-Before-You-Optimize.md, TGOPD.md, EmbodiedSkills.md, FlowBalance.md, browser-use.md]
reliability: high
---

# 검사 가능성 공사 (Inspectability Construction)

> [!insight] 2026-09-08 — **검사 장치의 "비용"과 "위치"가 처음으로 다뤄졌다** ([[TGOPD]]·[[EmbodiedSkills]]·[[FlowBalance]] 편입)
> 이 개념은 09-06에 **확정 승격**됐으나, 그때까지 남은 가장 실무적인 반론은 **"검사기는 공짜가 아니다"** 였다. 이번 배치가 그 반론에 **구체적 답 두 개**를 준다.
>
> **① 💰 검사 비용을 유휴 자원에서 조달한다 ([[TGOPD]])**
> 비동기 OPD에서 **교사 노드는 학생이 롤아웃하는 동안 논다.** TGOPD는 그 유휴 시간에 검증 프로브를 돌려 프롬프트별 교사 신뢰도를 추정한다.
> 실측: **교사 노드 GPU 사용률 9.8% → 78.9%** (4B 단일 도메인 실행 1회, 저자가 범위 명시).
> → **정식 항목 신설**: *새 검사 장치를 붙일 때, 그 비용을 시스템의 **유휴 자원**에서 조달할 수 있는지 먼저 본다.* 검사는 종종 **이미 사 놓고 안 쓰는 계산**으로 지불된다.
>
> **② 🚪 4단 관문 — 모델 출력을 "결정"이 아니라 "제안"으로 격하시킨다 ([[EmbodiedSkills]])**
> *"행동 예측이나 모델이 생성한 스킬 결정은 **그 자체로는** 제안된 조작이 현재 상태에서 유효한지를 보장하지 않는다."*
> → **제안 → 사전조건 검사 → 실행 → 사후 검증**을 **고정 인터페이스**로 강제. 부수 효과: 인터페이스가 계획·실행·검증·복구를 **구조화된 궤적으로 기록**하고, **그 기록이 다시 각 구성요소의 감독 신호**가 된다.
> → 이 개념의 확장: **검사 창을 지으면 로그가 남고, 로그는 학습 데이터가 된다.** 검사 가능성은 비용이 아니라 **자산을 생산**할 수 있다.
>
> **③ 🔴 2도메인 독립 수렴 — 이번 배치의 가장 강한 신호**
> [[TGOPD]](LLM 학습)와 [[EmbodiedSkills]](로봇 제어)는 **서로를 인용하지 않고 같은 골격에 도달**했다: **"신뢰할 수 있는지 먼저 검증하고, 통과할 때만 쓴다."**
> 한쪽은 *교사 신호*를 게이팅하고 한쪽은 *로봇 행동*을 게이팅한다. **도메인이 최대한 멀리 떨어져 있는데 처방이 같다** — 이 개념이 도메인 특수적 유행이 아니라는 근거로는 지금까지 중 가장 강하다.
>
> **④ [[FlowBalance]] — "붕괴"를 논증에서 측정으로 바꿨다**
> [[One-Symptom-Three-Levers]] 가 붕괴를 *"추론 경로 집합의 축소"* 로 **정의**했다면, FlowBalance는 그것을 **두 개의 측정 가능한 양**으로 조작화했다: **응답 길이 붕괴 회피** + **통제된 AIME24 진단에서 정답 전략 다양성**.
> → 이 개념의 핵심 문장(*"급소는 논증이 아니라 측정"*)의 새 사례.
> 그리고 [[TGOPD]] 는 **왜 내부 신호가 안 되는지**를 손실함수로 논증한다: *"엔트로피·우도 일치 같은 분포적 프록시는 불확실성이나 일치를 재지 **결과의 정확성을 직접 검증하지 않는다**"* + *"reverse KL이 mode-seeking이라 **확신에 찬 틀린 교사가 강한 오업데이트**를 낳는다."*
> → **외부 정답 고정 6번째 형태 추가**: 기하 · 실행결과 · 사람 유지보수 목록 · 버전 태그 문서 · 히든 평가 · **🆕 프롬프트별 사전 검증 게이팅**([[TGOPD]]).
>
> **⑤ 🪞 볼트 자신에게 적용 — 이번엔 구멍이 하나 메워졌다**
> 이 개념은 09-06부터 *"`queries/` 0건 = 자기가 쓴 위키를 자기가 읽고 판단 = 히든 평가에 해당하는 것이 없다"* 는 **볼트 미이행**을 달고 있었다. **09-08 현재도 `queries/` 는 0건이며, 미해소 6회차다.**
> 다만 **다른 구멍 하나는 이번에 발견되고 처방됐다**: [[browser-use]] 사건 — 볼트는 `[[browser-use]]` 라는 링크를 2개 문서에 써 놓고 **10개월간 페이지를 만들지 않았다.** 즉 **자기가 무엇을 참조하는지 볼 수 있는 계기판이 없었다.**
> → 처방: **매 인제스트 종료 시 "그 회차에 새로 생긴 댕글링 링크"만 목록화**(전수 963페이지 감사보다 훨씬 싸고, 이 사건은 신규분만 봐도 잡혔다). **이것이 볼트가 자신에게 지은 첫 검사 창이다.**
>
> **⑥ ⚠️ 근거 등급표에 다섯 번째 칸이 필요하다**
> 09-07에 3등급(독립검증 / 자체측정 / 지표전무)을 세웠는데, 이번 배치에 **수치가 없는 것이 정상인 소스가 2건** 나왔다: [[hyperframes]](렌더러 — 품질 벤치가 성격상 해당 없음) · [[One-Symptom-Three-Levers]](리뷰 — 기여물이 어휘 통일).
> → **"메타/구조 기여(수치 없음이 정상)"** 칸 신설. **근거 등급을 매기기 전에 주장의 종류를 먼저 분류한다** — 이걸 안 하면 [[camofox-browser]](수치가 있어야 하는데 없음)와 [[hyperframes]](없어도 됨)를 같이 감점하게 된다.


> [!insight] 한 줄 정의
> **모델이 잘하는지 묻기 전에, 모델이 무엇을 들고 있는지 볼 수 있게 만들어라.** 성능이 아니라 *관측 창* 자체를 설계 목표로 삼는 흐름.

## 왜 개념으로 세우는가 — 그리고 왜 아직 "잠정"인가

2026-09-02 배치의 HF 논문 5편 중 **3편이 서로를 참조하지 않은 채** 같은 방향을 향했다. 세 편 모두 *성능 개선*을 앞세우지 않고 **내부 상태를 외부에서 검사 가능한 형태로 노출하는 장치**를 핵심 기여로 내놓는다.

- **[[Qwen-Drive-1.0]]**(자율주행 / 표현 층) — 외부 BEV 헤드를 성능 부품이 아니라 **탐침(probe)** 으로 규정한다. 원문: *"serves as a **probe** of the 3D information accessible from the shared representations and provides an **explicit, inspectable interface** to 3D scene structure."* 공유 표현 안에 3D 정보가 실제로 있는지 **밖에서 확인할 수 있게** 만드는 장치다.
- **[[Safin-1]]**(안전성 / 메모리 층) — 메모리를 *"과거 맥락의 수동적 기록"* 에서 **"행동을 유지·진화시키는 능동적 기질(active substrate)"** 로 재정의한다. 라우팅되는 상태가 곧 **조작·관찰 가능한 인터페이스**가 된다.
- **[[UI-Venus-2]]**(GUI 에이전트 / 보상 층) — **검증(Verification)을 환경·태스크와 나란한 스케일링 축으로 승격**시킨다. 시각 키포인트 + 다중모델 투표 기반의 **트레이스 단위·샘플 단위 평가자**를 학습 인프라로 짓는다.

> [!warning] ⚠️ 잠정 분류 — 승격 조건 명시
> 볼트 규칙(08-31 신설) *"단일 시점의 방향 전환은 2회 연속 확인 전까지 해석하지 않는다"* 를 적용한다. 이 개념은 **한 배치의 관측**이며 세 논문은 **도메인·층위가 모두 다르다**(주행 표현 / 안전 메모리 / GUI 보상). 공통점을 내가 사후에 부여했을 가능성을 배제하지 못한다.
> **승격 조건**: 다음 배치에서 *"내부 상태를 검사 가능하게 만드는 것 자체를 기여로 내세우는"* 논문이 **2건 이상** 추가 관측되면 정식 개념으로 확정한다. 그 전까지 **reliability: medium**.

## [[국소-수리-원리]]와의 관계 — 전제와 본체

이 개념은 독립적이지 않다. **[[국소-수리-원리]]의 전제 조건을 짓는 공사**다.

그 원리는 *"잘된 부분은 건드리지 말고 틀어진 지점만 고쳐라"* 인데, 08-31에 세울 때부터 단서가 붙어 있었다 — **"틀어진 지점을 찾을 수 있어야 성립한다."** 찾을 수 없으면 무엇을 보존하고 무엇을 고칠지 결정할 수 없다.

- **국소 수리**: *어디를 고칠지 정해졌을 때* 어떻게 고칠 것인가 (학습·추론·평가·커리큘럼·아키텍처 5층위)
- **검사 가능성 공사**: *어디가 틀어졌는지* 를 애초에 볼 수 있게 만드는 층

**두 흐름이 한 배치에서 동시에 관측된 것**이 이번 배치의 구조적 발견이다. [[Safin-1]]은 **양쪽 모두에 속한다**(백본 보존 = 국소 수리 / 메모리 인터페이스 = 검사 가능성).

## 🚨 이 개념의 급소 — 검사자는 누가 검사하는가

관측 창을 만드는 것과 **그 창이 진실을 보여주는 것**은 다르다. [[Agentic-Artifact-Creation]](259편 서베이)의 부정적 결론이 여기를 정확히 찌른다 — **"LLM 심판이 생성기와 선호·맹점을 공유하면 독립 증거가 못 된다."**

- [[UI-Venus-2]]의 **다중모델 투표**는 이 문제에 대한 직접 처방이지만, **투표 참여 모델들이 서로 독립인지는 초록에 없다.** 같은 데이터로 학습된 모델들의 투표는 다수결이 아니라 **같은 편향의 증폭**일 수 있다.
- [[DiagEvo]]는 더 노골적이다 — 문항 판정을 **솔버 자신**에게 맡긴다(이중 신뢰도 필터링). 생성기와 심판이 *같은 계열*이 아니라 **같은 모델**이다.
- [[Qwen-Drive-1.0]]의 BEV 탐침은 이 함정을 **구조적으로 회피**한다. 탐침이 LLM이 아니라 **기하학적 과제**(3D 탐지·점유 예측·맵 분할)이기 때문이다. **정답이 모델 바깥에 있다.**

> [!insight] 여기서 나오는 실무 규칙
> **검사기가 피검사자와 같은 실패 모드를 공유하면 그것은 계기판이 아니라 거울이다.**
> 좋은 탐침의 조건은 정교함이 아니라 **독립성** — 정답이 모델 바깥의 무언가(물리·기하·실행 결과)에 고정돼 있는가. [[Qwen-Drive-1.0]]식 접근(기하 과제로 탐침)이 [[DiagEvo]]식 접근(자기 투표)보다 이 기준에서 낫다.

## 이 볼트에 적용하면

볼트 자신이 이 문제를 겪었다. **08-30에 내린 "원본이 GGUF를 추월" 결론이 08-31에 철회**됐는데, 원인은 분석 능력 부족이 아니라 **동결된 카운터를 알아볼 관측 창이 없었다**는 것이었다. 그리고 그 판별은 **다른 모델([[LTX-2.5]])이라는 외부 대조군**으로 이루어졌다 — 정확히 위의 "독립성" 조건이다.

- ✅ **적용 중**: 수치 교차 확인(둘 이상 동시 0이면 파이프라인 문제) · raw ↔ API 드리프트 상시 대조 · 초록 원문 대조
- ⚠️ **미비**: 포크 8,975건([[OpenClaude]])의 **성격**(커스터마이즈 vs 아카이빙)을 판별할 필드가 없다 — 관측 창의 공백이 이번 배치에서 드러났다

---

## 🎓 2026-09-06 — **잠정 → 확정 승격 (승격 조건 충족)**

09-02에 이 페이지가 명시한 승격 조건은 **"다음 배치 2건 추가"** 였다. 09-05·09-06 배치에서 **2건이 도달했고, 둘은 자매 논문이며 제1저자 3인이 같다.**

- **[[Aspire]]** (2608.31111 · 업보트 210) — **급소를 실험으로 확인한 첫 사례.** 자기진화 에이전트의 실패 원인 3개 중 2번이 정확히 이 개념의 급소다: *"에이전트가 **좁은 자체평가를 신뢰**해서 국소 향상이 **히든 평가로 전이되지 않는다**."* 그리고 해법도 이 개념과 정합적이다 — 자기평가를 금지한 게 아니라 **자기평가를 채점하지 않고**, 정답을 모델 바깥(전문가 작성 히든 520문항)에 고정했다.
- **[[HarnessDev]]** (2609.01437 · 업보트 240) — **히든 평가 태스크를 개발 과정에서 분리**(withheld from development)하고, 능력과 **효율(실행 토큰 비용)** 을 함께 잰다. 성능만 재면 비용으로 성능을 사는 하네스가 이긴다는 것을 설계로 막았다.

> [!insight] 승격의 근거는 개수가 아니라 **급소가 실험으로 확인됐다는 것**이다
> 09-02 원문의 급소는 이랬다 — *"검사기가 피검사자와 같은 실패 모드를 공유하면 계기판이 아니라 거울이다. 정답이 모델 **바깥**에 고정돼야 유효하다."*
> 그때 이건 **논증**이었다. [[Aspire]]는 그것을 **부정적 결과로 측정**했다: 자체평가를 믿은 에이전트의 국소 향상이 히든 평가로 **전이되지 않았다**. 거울을 보고 있었다는 게 숫자로 나온 것이다.
> → **reliability medium → high**, 분류 **잠정 → 확정**.

### 외부 정답 고정의 사례가 다섯 형태로 정리된다

| 형태 | 외부 기준 | 소스 |
|---|---|---|
| 기하 | BEV 3D 구조 | [[Qwen-Drive-1.0]] |
| 실행 결과 | 프로그램 검증기 | [[DeepSWE]] |
| 사람이 유지보수하는 목록 | Wikipedia WikiProject | [[humanizer]] |
| **버전 태그가 붙은 공식 문서** | 라이브러리 문서 | **[[context7]]** *(2026-09-06 신규)* |
| **개발 중 볼 수 없는 히든 평가** | 전문가 작성 문항 | **[[Aspire]] · [[HarnessDev]]** *(2026-09-06 신규)* |

마지막 형태가 이전 넷과 다른 점: 앞의 넷은 **정답의 출처**를 밖에 두지만, 히든 평가는 **정답을 볼 수 있는 시점**을 밖에 둔다. **접근 통제도 외부화의 한 형태**라는 것 — 개념이 한 겹 넓어졌다.

> [!warning] ⚠️ 승격했으나 남는 급소 — **누가 히든 평가를 쓰는가**
> [[Aspire]]의 520문항은 *"전문가 작성"* 인데 **그 전문가가 누구인지 초록에 없다.** 정답을 밖에 두는 데 성공하려면 **바깥이 안쪽보다 신뢰할 만해야** 하고, 그 신뢰는 이 개념 자체로는 조달되지 않는다.
> 그리고 **[[HarnessDev]]가 새 한계를 하나 더 붙였다**: 하네스 개선이 **모델 간 전이가 제한적**이다. 검사 창을 잘 지어도 **그 창은 특정 모델용**일 수 있다.

### 볼트 자신에 대한 적용 — 미이행 항목이 그대로다
- ✅ **적용 중**: 수치 교차 확인 · raw ↔ API 드리프트 상시 대조(이번 배치에서 **09-05 raw값 15건 전부 재측정**해 드리프트 확인) · 초록 원문 대조
- ⚠️ **미비 유지**: `queries/` **0건** — 볼트는 자기가 쓴 위키를 **자기가 읽고 판단**한다. [[Aspire]]가 말한 *"좁은 자체평가"* 의 구조와 정확히 같다. **히든 평가에 해당하는 것이 볼트에 없다.**

---
## 🆕 2026-09-07 — **논문 5건 중 4건이 평가 장치를 함께 냈다** (이 개념의 최대 밀도 배치)

> [!insight] 벤치마크·데이터셋 동반 공개가 **예외에서 관행으로**
> | 소스 | 함께 공개한 것 | 성격 |
> |---|---|---|
> | [[Motion-Omni]] | **SwDA-500** + *"확률적 개방형 전신 음성대화 **최초의 공개 평가 프로토콜**"* | 시스템 간 **오디오를 맞춘 뒤** 렌더링·자동지표·인간평가·지연을 통일 |
> | [[WorldSculpt]] | **UE-MeshyScene** — 수백 오브젝트·**오브젝트별 주석**·**GT 메시** | 밀집 가림 장면의 조합적 생성 평가 |
> | [[Enoki]] | **EnokiQA** — claim 검증 ↔ span 위치추정이 **정렬된** 이중 입도 데이터셋 | 정렬 단계를 없앤 방법이 **정렬된 평가셋**을 냄 |
> | [[Ask-Before-You-Optimize]] | **OR-Clarify** — 슬롯을 숨기고 시뮬레이션 사용자와 제한 상호작용 | **측정된 적 없던 능력**을 잴 수 있게 만듦 |
>
> **[[Motion-Omni]] 의 프로토콜이 특히 이 개념의 정수다** — *"모션 시스템 간 **오디오를 맞춘 뒤** 비교한다"*. 통제 변수를 고정하지 않은 기존 비교가 무엇을 재고 있었는지 되묻게 만든다.

> [!insight] **[[Ask-Before-You-Optimize]] 는 이 개념을 한 단계 뒤집는다 — 평가의 전제가 능력의 지도를 만든다**
> 초록 원문: *"Existing evaluations **largely assume a complete specification** and therefore **overlook whether an agent knows when clarification is needed**."*
> 이 개념은 지금까지 *"무엇을 검사 가능하게 만들 것인가"* 였다. 이 논문은 **"검사되지 않아 존재가 가려진 능력"** 을 드러낸다.
> 게다가 평가 설계가 정교하다 — **슬롯 복구율**만 재면 *무조건 많이 묻는* 에이전트가 이기므로 **상호작용 비용**·**중단 행동**·**암묵적 가정(silent assumptions)** 을 함께 잰다. **묻지 않는 실패와 너무 묻는 실패를 동시에 처벌**한다.

> [!warning] 🔴 그런데 **같은 배치가 반대 극단도 보여준다 — 자체 배지와 자체 측정**
> | 소스 | 근거의 형태 | 판정 |
> |---|---|---|
> | [[open-science]] | *"BiomniBench-DA Public 50 **1위**"* — **저자가 자기 레포에 단 배지**. 방법·표본 없음 | 🔴 **성능 주장으로 인용 금지** |
> | [[Huihui-Qwen3.8-27B-abliterated-GGUF]] | **벤치마크 전무.** DL 219만이 유일한 숫자 | 🔴 다운로드는 **수요**를 재지 성능을 재지 않음 |
> | [[Tiel-Coder-35B-A3B-GGUF]] | 자체 측정이나 **통제군 포함**·자기 열세 3분해 공개 | 🟡 **조건부 인용 가능**(n=25 명시 필수) |
>
> **[[Tiel-Coder-35B-A3B-GGUF]] 가 이 개념의 실무 기준선을 보여준다** — *"the same quant carrying Ornith's own template **scores exactly what Ornith scores**"* 라는 **대조군 한 줄**이 자체 측정을 인용 가능하게 만든다. [[Repo-To-Skill]] 이후 **재배포자 층에서는 첫 사례**.
> ⚠️ 단 **표본 25**다. **1문제 = 4%p** 이므로 순위는 재실행 시 뒤집힐 수 있다. **통제군이 있어도 표본이 작으면 결론은 약하다.**

### 볼트 자신에 대한 적용 — 09-07 현황
- ✅ **적용 중**: raw ↔ API 드리프트 **전건 대조**(13건) · 초록/README **원문 대조** · 계열 시계열 추적으로 09-03 가설 **검증**([[timesfm-3.0-pytorch]])
- 🔴 **이번 회차에 드러난 새 구멍**: **내용(구성) 대조 부재.** [[humanlayer-skills]] 를 raw가 *"스킬 3종"* 이라 적었고 **실제는 5종**이었다. 지표는 대조했는데 **내용은 대조하지 않고 있었다.**
- ⚠️ **미비 유지**: `queries/` **0건** — 볼트는 여전히 자기가 쓴 위키를 자기가 읽고 판단한다. [[Aspire]] 의 *"좁은 자체평가"* 구조 그대로.

## 관련 페이지
- [[Motion-Omni]] · [[WorldSculpt]] · [[Enoki]] · [[Ask-Before-You-Optimize]] · [[Tiel-Coder-35B-A3B-GGUF]] · [[open-science]] — **2026-09-07 신규**
- [[국소-수리-원리]] — 이 개념이 그 원리의 **전제**를 짓는다
- [[Qwen-Drive-1.0]] · [[Safin-1]] · [[UI-Venus-2]] — 근거 3편
- [[Agentic-Artifact-Creation]] — 심판 비독립성 경고
- [[DiagEvo]] — 급소에 걸리는 반례
- [[Aspire]] · [[HarnessDev]] — **승격 근거 2편**(히든 평가 분리)
- [[context7]] — 외부 정답 고정의 네 번째 형태(버전별 공식 문서)
- [[LLM-Wiki]] — 볼트 자신의 좁은 자체평가 문제
- [[ai-news]]
